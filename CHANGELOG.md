# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- **`bin/olladroid` CLI wrapper.** First-class command-line surface so scaffolding reads as `olladroid new spell-bee` instead of `node cli/new.js --slug spell-bee`. Zero-dep Node dispatcher (Node 18+ built-ins only) that routes subcommands into the existing `cli/new.js` and `cli/update.js` modules via `require()` — one process, one exit code, no bash/node boundary. `--version` / `-v` reads `SDK_VERSION` from `sdk/olladroid.js` so the wrapper can never drift from the SDK. `--help` / `-h` / no-args print usage; unknown subcommands exit 2 with usage on stderr. `scripts/install-ollama.sh` appends an idempotent `export PATH="$HOME/olladroid/bin:$PATH"` to `~/.bashrc` (guarded by a comment marker so re-running the installer doesn't stack duplicate exports). 12 new unit tests at `bin/test/olladroid.test.js` spawn `bin/olladroid` as a child process and verify the real stdout/stderr + exit code surface for version, help, routing, and error paths. CI `cli-tests` job now runs `node --test bin/test/*.test.js` alongside `cli/test/*.test.js`. README Quick Start and CONTRIBUTING template-testing recipe updated to lead with `olladroid new` (with a note that `node cli/new.js` still works — the wrapper is thin). Scaffold-drift CI continues to invoke `node cli/new.js` directly for reproducibility. **185 tests pass (173 + 12 new).**

### Changed
- **`docs/index.md` landing page brought up to v0.3 parity.** The Jekyll landing page at `s1dd4rth.github.io/olladroid` was still the old v0.1.0 walkthrough with the "Run a Free, Private AI on Your Old Android Phone" title and — worse — a broken install step that told readers to navigate Chrome to `file:///sdcard/olladroid/pwa/chat.html`, which doesn't work because service workers don't run on `file://` origins. The frontmatter, H1, and hero are now repositioned to match the README ("the AI app framework that fits in one phone"), with the olladroid logo embedded at the top, the `curl|bash` one-liner lifted to the top of the page as a new "Quick install (one command)" section, and Step 6 rewritten from the broken `adb push` + `file://` flow to the working `scripts/start-ollama.sh --chat` flow that serves on `http://localhost:8000/` and opens the launcher. New **Step 8: Build your own AI mini-apps** section walks readers through `olladroid new` interactively and non-interactively, documents `olladroid update` as the SDK-refresh escape hatch, and names both reference templates (Spell Bee kids-game, Summariser productivity). "What you'll end up with" hero bullets expanded from the single chat UI to the full v0.3 surface (launcher + two templates + scaffolder CLI). Stale `/sdcard/start-ollama.sh` and `/sdcard/setup-autostart.sh` paths updated to `~/olladroid/scripts/`. Conclusion rewrites to talk about the framework framing and cites the LG G8 ThinQ validation run + 185-test passing baseline.

## [0.3.0] - 2026-04-16

**Highlights:** v0.3.0 is the public-launch release. Four big things land together. **(1)** The project renamed from `ollama-pocket` to `olladroid` to make the scope honest — it's Ollama on Android, which is what it actually is, and the new name stands on its own without renting space in the Ollama namespace. **(2)** The scaffolding system gets its second reference template, `templates/productivity/summariser/`, alongside `templates/kids-game/spell-bee/`. Two templates in two categories earns the "platform" framing; one was a bespoke kids game. **(3)** A new TE-styled PWA launcher at `pwa/index.html` reads a runtime manifest `pwa/apps.json` and renders a tile grid — the v0.1.0 chat UI plus every scaffolded mini-app, all under one home screen at `http://localhost:8000/`. Scaffolded apps now default to `pwa/apps/<slug>/` and auto-register their launcher entry at scaffold time. **(4)** A real brand identity — the olladroid llama wordmark — lands as `pwa/logo.svg`, wired into both the README hero and the launcher header. On top of that, the README gets a v0.3 overhaul with an updated Mermaid architecture diagram (launcher as the entry point, with both reference templates as tile children), a "Will this work on my phone?" preflight table scoped to the one device we actually measured (LG G8 ThinQ / SD855 / 5.5 GB RAM), and a Gallery section embedding both the launcher screenshot and the existing Spell Bee storyboard. Validated end-to-end on the LG G8 against real `qwen2.5:1.5b` through Ollama 0.20.5 — launcher renders 3 tiles, Summariser returns valid `{tldr, bullets, key_points}` JSON, Spell Bee's 5-state FSM transitions cleanly, `window.Olladroid` exposes its 10-key surface. **173 tests pass. Scaffold-drift zero for both examples. Shellcheck clean.**

### Changed
- **README v0.3 overhaul.** Architecture diagram (Mermaid) now shows the launcher as the entry point with the chat UI and two scaffolded mini-apps as its children — honestly reflecting the post-PR-27 runtime shape. New "Gallery" treatment at the top of the Demo section embeds the launcher screenshot (`demo-assets/storyboard-00-launcher.png`, committed) above the existing Spell Bee storyboard. New "Will this work on my phone?" preflight table sits above Model Recommendations — explicitly scoped to the one device we've actually measured (LG G8 ThinQ, Snapdragon 855, 5.5 GB RAM) with honest projections for adjacent device classes. Fixes a pre-existing factual error on the Model Recommendations table: the speeds were correctly attributed to the LG G8 benchmark file but the table caption claimed "Snapdragon 865, LG V60" — both wrong. LG `LM-G850` = LG G8 ThinQ, SoC `msmnile` = Snapdragon 855. The Two-paths-same-install subsection rewrites to "One launcher, many apps" reflecting the unified flow, with a `--chat-direct` escape hatch noted for anyone who wants the v0.1.0 direct-to-chat behaviour. "What's Included" grows entries for `pwa/index.html`, `pwa/apps.json`, `pwa/apps/<slug>/`, `sdk/olladroid.js`, `cli/apps-manifest.js`, `cli/update.js`, `templates/_base/`, both reference templates, and `examples/`. This is a docs-only PR — no code changes, 173 tests still pass, drift still zero.

### Added
- **v0.3 track — PWA launcher + `apps.json` runtime manifest** (re-landed post-rename). New `pwa/index.html` is a Teenage Engineering-styled tile grid that lists every installed mini-app: the v0.1.0 chat UI as a fixed first tile, plus every template-scaffolded app under `pwa/apps/<slug>/`. Fetches `pwa/apps.json` on boot, renders a tile per entry (icon + name + description + category badge), falls back to slug initials if an icon fails to load, and does a best-effort `/api/tags` ping so the header shows `N MODELS` or `OFFLINE`. `scripts/start-ollama.sh` now opens the launcher by default; `--chat` is kept as an alias for backwards compat and `--chat-direct` is the new escape hatch for landing straight on `chat.html`. New `cli/apps-manifest.js` is the zero-dep read/write API for the manifest — stable key order, stable sort (`chat` builtin first, then builtins alpha, then user apps alpha), tolerates missing file by returning the default set. `cli/new.js` default output dir moves from `apps/<slug>/` to `pwa/apps/<slug>/` so scaffolded apps sit inside the served PWA tree, and both `cli/new.js` and `cli/update.js` now `upsertEntry` into `pwa/apps.json` after a successful scaffold. `registerScaffoldedApp` short-circuits for any output dir outside `pwa/apps/` so `examples/` drift-check runs, `/tmp` fixtures, and explicit `--output` paths never touch the runtime manifest. `pwa/apps/` is gitignored (per-user state) but `pwa/apps.json` itself is committed with the default chat entry so a fresh clone boots a populated launcher. 15 new unit tests in `cli/test/apps-manifest.test.js` cover default manifest shape, stable sort, canonical key order, upsert/remove, entry derivation, round-trip read/write, fresh-repo fallback, and the `registerScaffoldedApp` pwa/apps/ gate. **This is a re-land of the closed PR #25 on top of the olladroid rename — all naming uses `Olladroid` / `olladroid-*` / `olladroid:` from day one.** 173 tests pass total.

### Changed
- **Project renamed: `ollama-pocket` → `olladroid`.** The repo moved to `github.com/s1dd4rth/olladroid` (GitHub auto-redirects the old URL). "Pocket" was a phone-size constraint; "olladroid" makes the scope honest — it's Ollama on Android, which is what it actually is. The CLI is now `olladroid new <slug>` in spirit (invoke via `node cli/new.js` same as before — a standalone `bin/olladroid` wrapper lands in a follow-up PR). The SDK global moves from `window.Pocket` to `window.Olladroid`; `sdk/pocket.js` to `sdk/olladroid.js`; every `--pocket-*` CSS variable and `.pocket-*` class in `templates/_base/style.css` and the two reference templates renames to `--olladroid-*` / `.olladroid-*`; `localStorage` key prefix moves from `pocket:<slug>:state` to `olladroid:<slug>:state`; service worker cache name from `pocket-<slug>-v2` to `olladroid-<slug>-v2`; log/error prefixes from `[Pocket]` / `pocket:` to `[Olladroid]` / `olladroid:`. Device filesystem paths under `/sdcard/ollama-pocket/` and `~/ollama-pocket/` become `/sdcard/olladroid/` and `~/olladroid/`. Jekyll `baseurl` in `_config.yml` moves from `/ollama-pocket` to `/olladroid`. Shipped CHANGELOG entries for v0.1.x and v0.2.x are left alone — they describe the project as it was at the time. `examples/spell-bee/` and `examples/summariser/` regenerated (87709 and 70372 bytes respectively, both ~1.2 KB larger than before because the `olladroid-` prefix is 3 chars longer than `pocket-`). This PR supersedes the closed `feat/launcher` branch (PR #25) — the launcher work will re-land on top of the rename in a follow-up PR. 158 tests pass. Drift-check zero for both examples.

### Added
- **v0.3 track — Summariser template + `productivity` category.** The scaffolder now ships a second reference template from a second category: `templates/productivity/summariser/`. Summariser is a paste-text-in, get-structured-summary-out tool — 2000-char input cap, three-state FSM (`idle → thinking → showing_summary`, with an `error` branch for bad responses), one `structuredChat` call per summarise against the schema `{tldr: string, bullets: string[], key_points: string[]}`. Same Teenage Engineering chrome as Spell Bee (section labels, orange pulse indicator, square status badge), re-using `_base/style.css` tokens and reading per-app config from the inlined `APP_CONFIG` block. Persistence is minimal — the last successful summary is saved to `localStorage` under `summariser-<slug>` so closing and re-opening the PWA restores the most recent result. Includes a "Copy TLDR" action, a char counter that turns orange near the cap, a network-first boot check against `/api/tags`, and content-safety on list rendering (`textContent` only, never `innerHTML`). Unlike Spell Bee, Summariser has no session bookkeeping, no round counter, and no `ageGroup` field in `APP_CONFIG` — the field is dropped entirely for non-kids categories.
- **Scaffold drift CI covers Summariser too.** The `scaffold-drift` workflow now rescaffolds both `examples/spell-bee/` and `examples/summariser/` and fails on any byte-level diff in either. `CONTRIBUTING.md`'s "Scaffold drift check" section now lists both regenerate commands (productivity/summariser drops the `--age-group` flag). The committed `examples/summariser/index.html` is 69249 bytes with the pinned `--scaffolded-at 2026-01-01T00:00:00.000Z`.

### Changed
- **CLI: `--age-group` is now kids-game-only.** `cli/new.js` refactored so that the `--age-group` flag, the interactive age-group prompt, and the `opts.ageGroup` → `APP_CONFIG.ageGroup` field are all gated on a new `CATEGORIES_REQUIRING_AGE_GROUP` set (currently just `kids-game`). Productivity and creative templates scaffold without it. `validateFlags` refuses productivity + an invalid `--age-group` if one is passed anyway (catches typos). `cli/scaffold.js` `buildAppConfig` omits the `ageGroup` key entirely when not provided — rather than carrying a dangling `null` through APP_CONFIG — while preserving the original field insertion order for kids-game so the existing `examples/spell-bee/` byte-compare drift check still passes. `cli/update.js` symmetric. The "Spell Bee may fail mid-round" warning in the interactive model-detection flow is now template-generic ("scaffolded apps that use structured JSON may fail"). 11 new unit tests cover the conditional path: productivity without `--age-group`, productivity rejecting a malformed `--age-group`, APP_CONFIG omitting `ageGroup` for productivity, field-order preservation for kids-game, and an end-to-end summariser scaffold that verifies the three result-card DOM IDs are wired. 158 tests pass.
- **README + Jekyll description repositioning.** The top-level story is now "the AI app framework that fits in one phone — offline, private, yours" rather than "a free private AI on your old Android phone". The scaffolding system is promoted to the lede; the original v0.1.0 "turn an old phone into a local AI server" story is preserved verbatim below the new hero as a still-supported use case. A new "Two paths, same install" subsection inside Quick Start explicitly names both paths (chat UI vs scaffolded app) so neither audience gets lost. The "Building Apps" section grows a "what every scaffolded app is" block listing the concrete properties (single HTML file, offline-first, installable PWA, agentic via structured JSON, byte-deterministic via CI drift). Jekyll `_config.yml` description updated so the GitHub Pages `<meta description>` and `og:description` tags carry the new framing. This is a docs-only change — no code, tests, or scaffolder behaviour moves. The positioning break is a deliberate pivot from the plan's "keep quiet until v0.3.0 with two templates" rule: one reference template + the scaffolder + the drift CI + full template-authoring docs + 147 tests is enough substance to tell the story honestly, and waiting for a second template before speaking was caution, not correctness.

## [0.2.1] - 2026-04-14

**Highlights:** the scaffolding system documented in [`requirements/plans/scaffolding-system.md`](requirements/plans/scaffolding-system.md) is now fully landed. Six PRs over two sessions ship a zero-dependency SDK (`sdk/olladroid.js`), a Node 18+ CLI (`node cli/new.js`) that turns HTML-comment-marker templates plus the inlined SDK into a self-contained installable AI app, a Teenage Engineering-styled `_base` layout, the first reference template (Spell Bee — a local spelling game for kids aged 4–12), a byte-deterministic `examples/spell-bee/` reference output policed by a new `scaffold-drift` CI job, and complete template-authoring docs in `CONTRIBUTING.md`. `scripts/bench.sh` shipped alongside as a fixed-prompt benchmark harness with three reference runs on the LG V60 ThinQ. Everything was validated end-to-end on a real phone (LG G8, Termux, Node v25.8.2) before each PR landed — the scaffold-drift job caught a real regression on its first production run, confirming it works as intended. **Per the plan's "keep quiet until v0.3.0" rule, the README top-level story is unchanged** — it still reads "Run a free, private AI on your old Android phone". The new "Building Apps" subsection lives below the fold, and the loud "local AI apps you can scaffold in seconds" announcement waits for v0.3.0 when a second template (Summariser / Quiz Master) earns the headline.

### Added
- **Scaffolding system, PR 6 of ~10 (docs)** — `README.md` gains a "Building Apps" subsection below "Model Recommendations" (not above the fold — the top-level story stays "run a free, private AI on your old Android phone"). It points at `node cli/new.js`, `node cli/update.js`, the committed `examples/spell-bee/` reference output, and the `CONTRIBUTING.md` template authoring docs. `CONTRIBUTING.md` adds two new sections: **"Adding a template"** documents the `templates/<category>/<name>/{body.html,app.js}` convention, the full HTML-comment marker reference (`APP_NAME`, `STYLE_INLINE`, `SDK_INLINE`, `APP_CONFIG`, `APP_BODY`, `APP_SCRIPT`) with per-marker escape rules, everything the base template gives you for free, and a local scaffold-then-serve test recipe. **"Scaffold drift check"** explains what `examples/spell-bee/` is for, why `--scaffolded-at 2026-01-01T00:00:00.000Z` is the pinned timestamp CI uses, and the exact regenerate-and-commit one-liner when the drift job fails. The "Testing" section also grows SDK / CLI `node --test` entries and a drift-section pointer. No code changes in this PR.
- **Scaffolding system, PR 5 of ~10 (examples + drift CI)** — new `--scaffolded-at <iso>` flag on `cli/new.js` pins `APP_CONFIG.scaffoldedAt` so the scaffolded output is byte-deterministic. Validation uses `Date.parse()` round-trip and normalises through `new Date(v).toISOString()`. Committed `examples/spell-bee/` as the reference scaffolder output generated with `--scaffolded-at 2026-01-01T00:00:00.000Z`. `examples/*/fonts/` is gitignored because fonts are verbatim copies of `pwa/fonts/`. New `scaffold-drift` CI job (Node 18 + 20 matrix) rescaffolds and runs `git diff --exit-code -- examples/`. The drift job caught real drift on its first production run, confirming it works as intended.
- **Scaffolding system, PR 4 of ~10 (Spell Bee + TE + sessions)** — real Spell Bee game loop replaces the placeholder stub with a 5-state FSM. Two `structuredChat` calls per round against word and judgment JSON schemas with an age-group-aware system prompt. Local `attempt.toLowerCase() === currentWord.toLowerCase()` override so a letter-perfect answer is always scored correct regardless of the model's verdict. Bounded 5-round sessions with explicit `Start Session` / `Next` / `New Session` buttons and a session-complete summary (Score / Accuracy / Best ever). YOU / WORD compare block with LCS-aligned character-level diff highlighting. Full Teenage Engineering design language mirroring `pwa/chat.html` — black/white/gray + `#ff5c00` orange, Space Mono + DM Sans fonts copied into each scaffolded app, square corners, uppercase letter-spaced labels, TE logo square. Network-first service worker (`fetch().catch(caches.match)`) replaces the previous cache-first version that shadowed re-scaffolded apps. `[hidden] { display: none !important; }` in `_base`.
- **Scaffolding system, PR 3 of ~10 (_base layout)** — `templates/_base/style.css` grows from a placeholder into the real mobile-first layout: design tokens as CSS variables (`--olladroid-*`), spacing + type + radius scales, system font stacks, sticky `.olladroid-header` with `backdrop-filter`, safe-area-aware padding, 48 px tap targets, `:focus-visible` outline, `.olladroid-banner` / `.olladroid-stack` / `.olladroid-card` utilities, `prefers-reduced-motion: reduce` guard. Regression tests pin the tokens so replacing `_base` with a placeholder would fail CI loudly.
- **Scaffolding system, PR 2 of ~10 (CLI skeleton)** — `cli/new.js`, `cli/prompts.js`, `cli/scaffold.js`, `cli/update.js`, `cli/models.js` ship the end-to-end scaffolder with both an interactive mode and a flag-driven `--non-interactive` mode. Node 18+ built-ins only — zero `npm install`. HTML-comment-marker substitution engine with per-marker escape rules: `escapeInlineScript()` neutralises stray `</script>` / `</style>` sequences, `safeJSONForHTMLScript()` escapes `APP_CONFIG`. `cli/update.js` re-inlines the SDK into an already-scaffolded app while preserving the embedded `app-config` block. 82 new CLI unit tests via `node --test`. `.github/workflows/ci.yml` gains a `cli-tests` job on Node 18 + 20. Also shipped two device-caught fixes: `escapeInlineScript()` (catches the SDK's own `</script>` reference in a comment), and wrapping `fetch` in a forwarder closure in `sdk/olladroid.js` so Chrome's WebIDL-backed `window.fetch` doesn't throw `Illegal invocation`.
- **Scaffolding system, PR 1 of ~10** — the shared SDK at `sdk/olladroid.js` plus a full `node --test` unit test suite. `sdk/olladroid.js` is the UMD-lite module that every scaffolded app will inline — exports `OllamaClient` (HTTP client with `ping` / `version` / `models` / `chat` / `streamChat` / `structuredChat`), `SessionManager` (localStorage-backed history + arbitrary state with an in-memory fallback for Safari private mode / Node / incognito), `EventBus` (tiny pub/sub), `pickModel` (regex-preference-list matcher that excludes `gemma3:1b` and `smollm2:360m` from the structured-output whitelist), `safeJSONForHTMLScript` (escapes `<`/`>`/`&`/U+2028/U+2029 to prevent `</script>` injection via the scaffolded `<script type="application/json">` config block), `compareSemver`, and `StructuredChatError`. **`sdk/test/`** is 58 unit tests across 5 files (`client`, `session`, `bus`, `pickmodel`, `safejson`) run via Node 18+ built-in `node --test` — zero npm deps. Tests cover happy paths plus the gnarly edges: stream chunk boundaries, malformed NDJSON mid-stream, abort via `onChunk → false`, structuredChat retry + StructuredChatError, version memoization, version warning fires exactly once, storage throwing on read/write, multiple SessionManager keys, EventBus once+off during emit. **`.github/workflows/ci.yml`** adds a new `sdk-tests` job with a matrix over Node 18 and 20 so any accidental Node-20-only API breaks CI at review time.
- **`scripts/bench.sh`** — benchmark harness for the local Ollama server. Runs a fixed prompt set from `benchmarks/prompts.json` (5 prompts covering minimal/structured/code/reasoning outputs) and computes per-prompt tok/s from Ollama's `eval_count` / `eval_duration` response fields. Reports cold vs warm runs separately (the first call is always cold — the script unloads any in-memory model via `keep_alive: 0` before running). Writes a markdown report to `benchmarks/<device-slug>.md` where the slug is auto-derived from `ro.product.manufacturer` + `ro.product.model` + `ro.board.platform`. Supports `--model`, `--host`, `--runs N`, `--output`, `--dry-run`.
- `benchmarks/` directory with `README.md` (contribution workflow), `prompts.json` (versioned fixed prompt set), and the first committed benchmark at `benchmarks/lge-lm-g850-msmnile.md` (LG V60 ThinQ on Snapdragon 865 — **7.38 tok/s warm median** for `qwen2.5:1.5b`, generated via `bench.sh --runs 2` on a real phone).
- `.github/ISSUE_TEMPLATE/benchmark_contribution.md` — structured template for submitting a benchmark, with a verification checklist covering phone thermal state, plugged-in/battery, background apps, and prompts.json version.
- `CONTRIBUTING.md` section documenting the benchmark workflow and quality bar.
- README "Model Recommendations" table now cites the measured 7.38 tok/s number from the first committed benchmark, with a link to the markdown file.
- **`scripts/bench.sh` filename now includes the Ollama model slug** (`lge-lm-g850-msmnile-qwen2-5-1-5b.md` instead of `lge-lm-g850-msmnile.md`). Previously, running bench.sh against multiple models on the same phone silently overwrote the same file each time — caught while benching all three README-recommended models on the LG V60. Existing `benchmarks/lge-lm-g850-msmnile.md` renamed to the new scheme.
- **Two more reference benchmarks on the LG V60** (Snapdragon 865): `gemma3:1b` at **9.60 tok/s warm** and `smollm2:360m` at **12.72 tok/s warm**. Replaces the "estimate, needs bench" placeholders in the README Model Recommendations table with real numbers — all three recommended models now have measured tok/s on the same phone, same prompt set, same conditions.
- `benchmarks/README.md` documents a known VmRSS limitation: the current single-process reading via `pidof ollama | awk '{print $1}'` picks only one of Ollama's two processes (`ollama serve` + `ollama runner`), and is suspiciously low for smaller models where it happens to pick the wrong one. Follow-up PR will sum VmRSS across all ollama PIDs. Does not affect tok/s numbers, which are the headline measurement.

## [0.2.0] - 2026-04-12

**Highlights:** the PWA chat UI now actually works as a PWA (served over HTTP with self-hosted fonts, installs to the home screen, works in airplane mode with real local inference); `scripts/debloat.sh` is vendor-aware with plain-text manifests under `debloat/` (closes #8); and the install path is now a **one-line paste** — `curl -fsSL https://s1dd4rth.github.io/olladroid/install.sh | bash` in Termux. Every change was validated end-to-end on a real LG V60 ThinQ (Android 12) before merging, which surfaced — and fixed — four latent v0.1.0 regressions and two new UX issues that would have shipped otherwise.

### Added
- **One-liner `curl | bash` install** (closes the "Quick Start has 4 lines of setup before you can run anything" gap). `curl -fsSL https://s1dd4rth.github.io/olladroid/install.sh | bash` in Termux is now the primary install path. The installer self-bootstraps — it detects that it's running from stdin (no local checkout available), pins the Termux mirror, `pkg install`s git, clones the repo to `~/olladroid`, and `exec`s itself from the clone so the rest of the flow has `pwa/` and `scripts/` available. Cuts the Quick Start from 4 lines of git clone + cd + bash to 1. Zero change in what gets installed — the second invocation runs exactly the same code path as the v0.2.0 manual-clone flow.
- `docs/install.sh` — small redirector served by GitHub Pages at `https://s1dd4rth.github.io/olladroid/install.sh` that fetches and execs the real `scripts/install-ollama.sh` from the `main` branch. Exists so the pasted URL is pretty (`s1dd4rth.github.io/olladroid/install.sh`) instead of a raw-githubusercontent.com URL. Users who prefer to inspect before running are pointed at the manual clone path in the README.
- `scripts/install-ollama.sh` self-bootstrap is idempotent from both paths: the bootstrap block is skipped when run from a local checkout, and re-running against an existing clone does a `git fetch origin main && git reset --hard origin/main` instead of re-cloning. No surprise destruction of the existing clone.
- **Vendor-aware debloat** (closes #8): `scripts/debloat.sh` now auto-detects the phone's manufacturer via `adb shell getprop ro.product.manufacturer` and loads the matching manifest from the new `debloat/` directory. v0.1.0 hardcoded ~60 LG-specific packages into the script itself, which meant running it on a Samsung / Pixel / Xiaomi device was a silent no-op for 80% of the list. Package lists are now plain-text manifests (one per line, `#` comments allowed) split by vendor (`debloat/lge.txt`) and by opt-in category (`debloat/social.txt`, `debloat/games.txt`, `debloat/google-apps.txt`). The default behaviour on an LG phone is unchanged; non-LG phones no longer see a wall of "Not found" for LG-specific packages.
- `debloat/README.md` — contribution workflow for adding a new vendor list. Every non-LG owner can fix the gap for their phone with a one-file PR. Bar: a `# Verified on: <device>, Android <N>` header is required.
- New flags on `debloat.sh`: `--list` (show available manifests without touching the device), `--vendor <name>` (override auto-detection), `--category <a,b>` (subset the opt-in bundles), `--no-categories` (vendor only), `--skip-categories <name>` (drop a specific category from the default set), `--save-report <path>` (machine-readable JSON report of what was removed / skipped / failed, device info, and the list of loaded manifests — useful for audit logs and for reviewer-friendly vendor list contributions), `--restore-from <report>` (**precise undo**: read a previous `--save-report` file and reinstall ONLY the packages that run removed, not the whole manifest).
- Documented the difference between `--restore` (broad: reinstall the whole manifest set) and `--restore-from` (precise: undo exactly one previous run). v0.1.0's `--restore` had the broad semantic silently — you could run `debloat.sh && debloat.sh --restore` and end up with MORE packages installed than you started with, because `--restore` reinstalled everything the manifest knew about, including packages removed by previous sessions. v0.2.0 keeps `--restore` for compatibility and adds `--restore-from` as the recommended "undo button" path. Found while running the full remove → restore round trip during PR #10 validation on an LG V60.
- `.github/ISSUE_TEMPLATE/debloat_contribution.md` — structured template for submitting a new vendor manifest, including a verification checklist.
- `CONTRIBUTING.md` section documenting the debloat contribution workflow and the bash 3.2 portability rule for shell scripts (macOS `/bin/bash` is still bash 3.2, so scripts must avoid associative arrays and handle empty array expansion under `set -u`).
- `CONTRIBUTING.md` with PR-based development workflow, conventional commits, shell style guide, and GitButler virtual-branch notes.
- GitHub pull request and issue templates under `.github/`.
- GitHub Actions CI running shellcheck on `scripts/`, with a status badge in the README.
- `.gitignore` covering macOS, editor, Jekyll, and Claude Code (`.claude/`) artifacts.
- **Self-hosted fonts** in `pwa/fonts/` — Space Mono (regular + bold) and DM Sans variable, ~70 KB total as WOFF2. Removes the runtime dependency on `fonts.googleapis.com` and is what actually makes "offline" mean offline. OFL 1.1 license shipped alongside (`pwa/fonts/OFL.txt` + `LICENSE.md`).
- `requirements/plans/B-pwa-install-path.md` — full tactical plan for the PWA HTTP fix.
- `requirements/plans/scaffolding-system.md` — v1 plan for the app scaffolding system.
- `requirements/REQUIREMENTS.md` — source requirements for the scaffolding system.

### Changed
- Repository merge strategy is now **rebase-merge only** — squash and merge-commit are disabled at the repo level to keep GitButler virtual branches sane.
- Docs anonymized: generic phone model, example LAN IP, and example ADB device serial in `docs/index.md` and `docs/medium.md`.
- **`scripts/start-ollama.sh --chat`** now serves the PWA over `http://localhost:8000` via `python3 -m http.server`, instead of launching Chrome on `file:///sdcard/olladroid/pwa/chat.html`. Service workers do not register on `file://` origins — the old path looked like a working PWA but could not cache offline, could not install to the home screen, and could not deliver any of the "works on airplane mode" story.
- `scripts/start-ollama.sh` launches the PWA via `am start -p com.android.chrome` so the user's default browser (Samsung Internet, Firefox, or whatever was last set) does not silently break the service worker path. Prints a manual-open hint if Chrome is absent.
- `scripts/start-ollama.sh` uses a `curl -sf` poll loop (2-second ceiling) to confirm the Python HTTP server is actually listening before launching Chrome, instead of sleeping 300 ms.
- `scripts/start-ollama.sh` LAN-IP detection now uses portable `ip -o addr show wlan0 | awk '{print $4}' | cut -d/ -f1` instead of `grep -P` (Perl-compatible regex is not guaranteed on BusyBox).
- `scripts/install-ollama.sh` now installs `python`, `iproute2`, and `curl` alongside `proot-distro`. `python` powers the local PWA HTTP server. `iproute2` provides the `ip` command used by LAN-IP detection, which was silently falling back to `"unknown"` in v0.1.0 on fresh Termux installs because `iproute2` is not in the default Termux package set. `curl` powers the PWA tarball fallback.
- `scripts/install-ollama.sh` now copies `pwa/` to `/sdcard/olladroid/pwa/` as part of the install, either from the local repo checkout or (fallback) from a GitHub tarball over HTTPS. This is what actually puts the chat UI on disk where `start-ollama.sh --chat` expects to find it.
- `pwa/sw.js` cache bumped from `ollama-v2` to `ollama-v3`. Cached asset list now includes the three WOFF2 font files. Old caches are wiped on SW activation. Fetch handler now uses stale-while-revalidate instead of network-first for static assets.
- `pwa/chat.html` now loads fonts via `@font-face` pointing at the local WOFF2 files, not via a `<link>` to `fonts.googleapis.com/css2?...`. Zero network calls to `fonts.gstatic.com` at runtime.
- README Quick Start rewritten around a Termux-native flow (`pkg install git && git clone && bash scripts/install-ollama.sh`). The ADB debloat path is still documented as an optional prerequisite for users who want to free RAM before installing Ollama.

### Fixed
- Broken GitHub URL in `README.md` and `docs/*` (`s1dd4` → `s1dd4rth` typo).
- `scripts/install-ollama.sh` no longer appends duplicate `export PATH=...` lines to `/root/.bashrc` on re-run. Guarded with a `grep -q` idempotency check.
- `scripts/install-ollama.sh` no longer writes a hardcoded `~/start-ollama.sh` stub that shadowed the real `scripts/start-ollama.sh`. The stub only knew how to run `ollama serve` and confused every user who tried to follow the README's `--wifi --chat` instructions. There is now exactly one authoritative start script.
- **Latent v0.1.0 regression:** LAN IP detection in `start-ollama.sh` was silently failing on fresh Termux installs because the `ip` command lives in the `iproute2` package which is not part of the default Termux set. The script was printing `http://unknown:11434` as the WiFi URL. Fixed by adding `iproute2` to the installer and documenting it.
- **Termux mirror roulette:** `install-ollama.sh` now pins `$PREFIX/etc/apt/sources.list` to `deb https://packages-cf.termux.dev/apt/termux-main/ stable main` *before* running its first `pkg update`. Termux's default behaviour is to pick a random mirror, and any given mirror can be broken on any given day. Real-hardware validation of v0.2.0 hit `mirror.textcord.xyz` returning a malformed clearsigned `InRelease` file, killing the install before step 2. Pinning to the official Cloudflare-backed mirror makes the install deterministic.
- **`proot-distro` Debian detection on 4.x:** the v0.1.0 detection used `proot-distro list | grep "debian.*installed"`, which never matches on proot-distro 4.x because the output format changed — plain `list` now returns the *catalog of supported distros*, not the installed ones. The installer always fell through to `proot-distro install debian`, which then errored with "already installed" and killed the script via `set -e`. Fix uses three detection paths in order: `proot-distro list --installed`, check for `$PREFIX/var/lib/proot-distro/installed-rootfs/debian/`, and finally catch the "already installed" error from the install command itself. Belt and suspenders.
- **PWA home screen icon was cropped to a circle:** the v0.1.0 `pwa/manifest.json` declared `icon.svg` with `purpose: "any maskable"`, but `icon.svg` is designed edge-to-edge — its white border and orange accent live outside the center 80% safe circle that Android's adaptive icon spec requires for maskable icons. LG's circular mask sliced off the border and the accent, leaving only "OL" letters in a plain black circle. Fix ships two icon variants: `icon.svg` (unchanged) declared with `purpose: "any"` for non-maskable contexts, and new `icon-maskable.svg` with a safe-zone layout declared with `purpose: "maskable"`. Service worker cache bumped to `ollama-v4` so existing installs re-cache the new asset list.

## [0.1.0] - 2026-04-12

### Added
- Initial release: run Ollama on Android via Termux + proot Debian.
- `scripts/install-ollama.sh`, `start-ollama.sh`, `setup-autostart.sh`, `debloat.sh`.
- PWA chat interface at `pwa/chat.html`.
- Jekyll-based documentation site under `docs/`.
